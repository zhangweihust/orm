<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

     <Button 
     android:id="@+id/btn_android"
     android:layout_width="fill_parent"
     android:layout_height="wrap_content"
     android:text="@string/android"
     android:layout_margin="10dp"
     />
    
  <Button 
      android:id="@+id/btn_androrm"
      android:layout_width="fill_parent"
      android:layout_height="wrap_content"
      android:text="@string/androrm"
      android:layout_below="@+id/btn_android"
      android:layout_margin="10dp"
      />
  <Button 
      android:id="@+id/btn_ormlite"
      android:layout_width="fill_parent"
      android:layout_height="wrap_content"
      android:text="@string/ormlite"
      android:layout_below="@+id/btn_androrm"
      android:layout_margin="10dp"
      />
    <Button 
      android:id="@+id/btn_greendao"
      android:layout_width="fill_parent"
      android:layout_height="wrap_content"
      android:text="@string/greendao"
      android:layout_below="@+id/btn_ormlite"
      android:layout_margin="10dp"
      />
</RelativeLayout>
